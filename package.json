{
  "name": "pontis",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "web",
    "worker",
    "webhook"
  ],
  "scripts": {
    "dev:api": "npm run dev -w api",
    "build:api": "npm run build -w api",
    "deploy:api": "npm run deploy -w api"
  },
"engines": {
    "node": ">=20"
  }
}
